export type Button = {
    text: string,
    href: string,
    icon: string,
}

export type Project = {
    heading: string,
    blurb: string,
    deployed?: string,
    github?: string,
    src: string,
}